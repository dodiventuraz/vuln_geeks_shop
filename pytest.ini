[pytest]
testpaths = tests
# Bisukan HANYA deprecation migrasi API Starlette TemplateResponse (posisi argumen).
# Pola pemakaian saat ini masih didukung penuh oleh versi yang di-pin; warning lain tetap tampil.
filterwarnings =
    ignore:The `name` is not the first parameter anymore.*:DeprecationWarning
